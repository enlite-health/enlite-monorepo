# e2e-prod — variáveis de ambiente. Copie para .env.local (gitignored) e preencha.
# Valores de PRODUÇÃO (enlite-prd). Confirmados pela recon 2026-07-11 (ver decisoes.md).

# URLs de produção (Cloud Run)
PROD_BASE_URL=https://app.enlite.health
PROD_API_URL=https://worker-functions-byh3gvl5yq-tl.a.run.app

# Firebase web SDK (enlite-prd). A apiKey web do Firebase NÃO é segredo — já é embarcada
# no bundle do frontend em produção. Segura pra ficar aqui.
FIREBASE_PROJECT_ID=enlite-prd
FIREBASE_API_KEY=AIzaSyByRp-NCY0m12iEoKyuIrV6vR49MZateXI
FIREBASE_AUTH_DOMAIN=enlite-prd.firebaseapp.com

# Credenciais do owner/admin (conta gabriel.g.stein@gmail.com, role=admin via custom claim).
# ISTO É SEGREDO — nunca commitar valor real. Setar em .env.local ou Secret Manager (Cloud Run Job).
# Necessário só para os smokes AUTENTICADOS (admin login/dashboards). Os smokes públicos não precisam.
E2E_ADMIN_EMAIL=
E2E_ADMIN_PASSWORD=
