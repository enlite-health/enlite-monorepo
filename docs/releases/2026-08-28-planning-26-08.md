# Release `release/planning-26-08` — 5 entregas da Planning de Produto de 26/08/2026

**Base:** `origin/main` `1246a662` · **Branch:** `release/planning-26-08` (merge `--no-ff` de 5 branches, 1 conflito resolvido em teste) · **Data:** 28/08/2026 · **Autor:** Gabriel Stein (com Claude Code).

Origem de tudo: ata `ebrain/reunioes/2026-08-26-admissao-de-paciente-ana-care-e-clickup.md` (ids `2026-08-26a#…`). Cada item tem task no ClickUp com **vídeo/prints anexados** e comentário de entrega.

## Régua de entrega usada (definida pelo Gabriel em 28/08)

1. Cobertura 100% nas linhas dos arquivos tocados. 2. E2E de integração **sem mock de API** (frontend real → `worker-functions` real no Docker → Postgres real → Firebase Auth **emulator** real). 3. Teste visual (`toHaveScreenshot`) nesse E2E. 4. Vídeo + print anexados na task.
Fornecedores externos são dublados **na fronteira** e nunca chamados de verdade (regra dura da casa: teste nunca toca canal real): Google Calendar (item 2) e WhatsApp — o worker de teste vai pelo canal Periskope contra o stub local na porta 9911 (item 5).

## Verificação integrada (nesta branch, após os merges)

| o quê | resultado |
|---|---|
| frontend `pnpm exec tsc --noEmit` / `pnpm lint` | 0 erros / rc=0 |
| frontend `pnpm test:run` | **233 arquivos / 4184 testes** após D200.1/D200.12 (nos merges: 231 / 4146; base `main`: 227 / 4104) |
| backend `npx jest --coverage` | **300 suítes / 4461 testes** após D200.1/D200.12 (nos merges: 296 / 4415; base: 296 / 4403) |
| E2E reais (Playwright `--project=integration`, API rebuildada desta branch) | **5/5** — `funnel-worker-new-tab`, `patient-status-aguardando-financeiro`, `filtros-combobox-listas-longas` (2), `kanban-reenviar` |
| e2e backend (Jest, Postgres real) | caso `PEND-09` ✓ (`admission-scheduling-roster.e2e.test.ts`) |

## As 5 entregas

### 1. Nome do prestador abre o perfil em **nova aba** — `feat/req05-nome-prestador-nova-aba` · ClickUp `86aj3yufn` · `2026-08-26a#REQ-05`
- **Pedido:** Javier — clicar no nome da tarjeta navegava para o perfil e obrigava a refazer o caminho; as recrutadoras abriam abas à mão. (A ata dizia "modal"; o Gabriel corrigiu para **nova aba**.)
- **Mudou:** `KanbanCard` e `VacancyFunnelTableRow` — o nome é `<a href="/admin/workers/:id" target="_blank" rel="noopener">`; sai o `navigate()`/`onWorkerClick`. Em nova aba o botão "Volver" do perfil cai no default `/admin/workers` (não há `location.state`).
- **Infra de teste:** `docker-compose.firebase.yml` ganha `FIREBASE_PROJECT_ID=demo-no-project` (o emulador ignora `--project`; sem isso nenhum login de staff via emulador funciona). Removido `funnel-worker-detail-nav.integration` (100% mockado, assertava a navegação antiga).
- **Prova:** 3 arquivos 100% linhas · E2E `funnel-worker-new-tab.integration.e2e.ts` 1/1 (lista e Kanban → popup no perfil real; aba original intacta) · 3 baselines · vídeo + 4 prints na task.

### 2. Solicitante de admissão entra **direto no Meet** — `fix/admission-meet-convidado-solicitante` · ClickUp `86ak7rtv9` · `2026-08-26a#PEND-09`
- **Pedido:** Ana/Marcel — quem faz a solicitação cai na sala de espera pedindo autorização ao anfitrião.
- **Causa real (não era config do Google):** o evento já convidava `patients.contact_email`; quando um **familiar** preenche o formulário (`requesterType='responsible'`), o e-mail vai para `patient_responsibles` e `contact_email` fica nulo → evento **sem convidado**.
- **Mudou:** `AdmissionSchedulingService` — a consulta do paciente traz o e-mail do responsável primário; o convidado é quem solicitou (paciente, senão responsável). Contrato do Calendar inalterado (`sendUpdates=all` já enviava).
- **Prova:** `AdmissionSchedulingService.ts` 100% em tudo · e2e Jest contra Postgres real (`admission-scheduling-roster.e2e.test.ts`, caso PEND-09 ✓; Calendar dublado na fronteira) · **sem vídeo/print** — backend puro, a tela pública não mudou (dito na task).
- **Só produção prova:** que o Google admite o convidado listado sem aprovação. Validação: 1 solicitação real de responsável → attendee no evento → entrar no Meet.

### 3. Estado `PENDING_ADMISSION` → **"Esperando financiero" / "Aguardando financeiro"** — `fix/patient-status-aguardando-financeiro` · ClickUp `86ajy085c` (recorte D195) · `2026-08-26a#DEC-10`, D195
- **Pedido:** Marcel — não existe "ativação pendente"; o estado leva o nome do motivo real da espera (o gate do financeiro, Diego 27/07).
- **Mudou:** i18n es/pt-BR da coluna do Kanban de pacientes **e** do badge da ficha (que dizia "En Admisión" para o mesmo estado — alinhado). Enum intacto; status de VAGA "Activación Pendiente" intacto.
- **Não mudou (decisão pendente):** o bucket "En admisión" do dashboard funde `ADMISSION`+`PENDING_ADMISSION` — renomear seria rotular errado; separar exige SQL no backend.
- **Prova:** `PatientIdentityCard` 100% linhas · E2E `patient-status-aguardando-financeiro.integration.e2e.ts` 1/1 · 2 baselines · vídeo + 2 prints.

### 4. **Combobox com busca** nas listas longas — `feat/req06-combobox-listas-longas` · ClickUp `86ak7rtxv` · `2026-08-26a#REQ-06`
- **Pedido:** Javier — "lista muy larga: usar el combobox… deletreando vas buscando".
- **Mudou:** reaproveitado `molecules/SearchableSelect` (o do filtro "Caso"; sem MUI — o frontend é Tailwind) com `inputSize="compact"` e `data-testid`. Provincia/Localidad dos filtros de prestadores e de vagas e os horários Desde/Hasta (48 opções) viraram combobox. Listas ≤10 seguem nativas.
- **Fora:** Cobertura (33) e Segmentos (17) do form de admissão só existem na `feat/campos-admissao-fase-5` — mesma troca ao mergear. Benchmark REQ-07 não entrou.
- **Prova:** 4 arquivos 100% linhas (`AdminWorkerProfileFilters` ganhou suíte própria) · E2E `filtros-combobox-listas-longas.integration.e2e.ts` 2/2 (digitar filtra; escolher manda `state=` na request) · 2 baselines · 2 vídeos + 3 prints.

### 5. Botão **"Reenviar"** na tarjeta + último envio — `feat/req08-reenviar-tarjeta` · ClickUp `86ak7rtwn` · `2026-08-26a#REQ-08`, DEC-19(b)
- **Pedido:** Marcel/Javier — um clique para redisparar a mensagem, com data/hora do último envio no card.
- **Achado:** em `main`, mover a tarjeta **não dispara mensagem** (`moveEncuadre` só grava a etapa); o convite existente tinha travas (cooldown 3 d + "já convidado 7 d") que negariam qualquer reenvio.
- **Mudou:** `VacancyInviteGuard` modo `resend` (mantém opt-out e throttle de não-resposta; `RESEND_COOLDOWN` de `MANUAL_RESEND_COOLDOWN_HOURS`=24 por pessoa×vaga); endpoint aceita `resend: true`; Kanban expõe `lastMessagedAt`; card com botão, "Último envío: dd/mm hh:mm"/"Sin envíos", estados e motivo de recusa localizado. Autoria `admin:<uid>` no log.
- **Decisões para validar:** janela de 24 h; manter o throttle; a mensagem reenviada é o convite de match (templates por etapa = DEC-12, task irmã).
- **Prova:** guard 100%; linhas tocadas cobertas nos demais (`MessagingController` 55,9%, `WJAFunnelController` 88%, `VacancyFunnelKanban` 65% do arquivo — o resto é código não tocado) · E2E `kanban-reenviar.integration.e2e.ts` 1/1 (stub recebeu exatamente 1 POST; log + `messaged_at` no banco; 2º clique recusado) · 2 baselines · vídeo + 3 prints.
- **D200.1 (`3d7b13f0`):** dentro da janela o botão já vem **desabilitado com o motivo e a hora em que abre** — a janela virou uma expressão SQL única (`resendCooldownUntilSql`, no guard) que o funil também usa; cada card recebe `resendBlockedReason { RESEND_COOLDOWN, until }` e o 422 carrega `until`. E2E 2× ✓ (após o 1º envio: `disabled`, "últimas 24 h", "Reenvío disponible desde"; SQL confirma `MAX(dispatched_at)+24h > NOW()`); baselines de página e de card.
- **D200.9 (`ddba6092`):** o convite **automático** (outbox) agora carimba `messaged_at` como o manual — antes o card dizia "Sin envíos" após o auto-invite (e, com a D200.1, com o botão travado). E2E de banco real `tests/e2e/outbox-messaged-at.e2e.test.ts` 2/2 (stub recebe 1 POST; `messaged_at` NULL → carimbado; log `source='outbox'`).
- **Monitor diário (`5250c13c`):** `OutboxProcessor` loga `outbox.messaged_at.updated/failed`; `e2e-prod/smoke/outbox-messaged-at.smoke.ts` vigia às 3h (falha > 0 nas 24 h acende; carimbos viram evidência no e-mail). Deploya sozinho no merge (`e2e-prod-monitor.yml`).
- **D200.11 (`1749038f`):** `vacancy-kanban-visual.e2e.ts` apagado (rota inexistente, 100% mockado, coberto por integração real).
- **D200.12 (`303478f6`):** cobertura **100% do arquivo** em tudo que a release tocou — `MessagingController` 56→100, `WJAFunnelController` 88→100, `VacancyFunnelKanban` 65→100, `useWJAFunnel` 80→100, e os ramos de `KanbanBoard`, `PatientIdentityCard`, `AdminMessagingApiService`, `VacancyFunnelTableRow`, `SearchableSelect`. Só testes. 5 ramos inalcançáveis documentados no commit (botão `disabled` não dispara `onClick`, fallback zod, guard redundante, método privado com 1 call site, `invitedAt` tipado string).

## Variáveis de ambiente novas / tocadas
- `MANUAL_RESEND_COOLDOWN_HOURS` (backend, default 24) — janela entre reenvios manuais.
- `FIREBASE_PROJECT_ID=demo-no-project` só no `docker-compose.firebase.yml` (stack de teste).

## Achados registrados nas tasks e NÃO consertados aqui
- ~~`vacancy-kanban-visual.e2e.ts` morto~~ — apagado nesta release (D200.11, `1749038f`).
- Stage não tem `ADMISSION_CALENDAR_ID_AR/BR` — booking de admissão não funciona lá.
- `worker-functions/node_modules` é symlink versionado apontando para caminho morto.
- ~~Auto-invite (outbox) não grava `messaged_at`~~ — consertado nesta release (D200.9, `ddba6092`).
- Piso global de 80% do `vitest.config` já reprova a base (40%) e o CI roda `pnpm test:run` sem coverage — piso inerte. `validate:lines` já reprova 3 arquivos na base (`AdminApiService` 442, `PhoneConflictModal` 445, `AdmisionPage` 576).
- Bucket "En admisión" do dashboard (ver item 3). `molecules/SelectField` duplica o átomo `Select`. `CaseSelectStep.tsx` é código morto.
- e2e do roster de admissão falha 4/11 em `main` no banco local (schema/índice da migration 283).

## Como reproduzir a verificação
```bash
cd worker-functions && docker compose -f docker-compose.yml -f docker-compose.test.yml -f docker-compose.firebase.yml up -d --build postgres firebase-emulator api   # API com --no-deps se o emulador ficar "unhealthy"
cd enlite-frontend && VITE_FIREBASE_PROJECT_ID=enlite-e2e-test VITE_FIREBASE_AUTH_EMULATOR=http://localhost:9099 VITE_API_WORKER_FUNCTIONS_URL=http://localhost:8080 pnpm dev
pnpm exec playwright test funnel-worker-new-tab patient-status-aguardando-financeiro filtros-combobox-listas-longas kanban-reenviar --project=integration
cd worker-functions && DATABASE_URL=postgresql://enlite_admin:enlite_password@localhost:5432/enlite_e2e USE_KMS_ENCRYPTION=false npx jest --config jest.config.e2e.js admission-scheduling-roster -t PEND-09
```
