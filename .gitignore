# Environment
.env
.env.local
.env.*.local

# Dependencies
node_modules/
# sem barra: com barra o padrao casa so DIRETORIO, e o symlink escapa
# (foi assim que worker-functions/node_modules entrou em git 2x: #173 e #231)
node_modules

# Build outputs
dist/
build/

# OS
.DS_Store

# Logs
*.log

# Secrets / credentials
.keys/
.mcp.json

# RAG local (vector DB + models cache)
.rag/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfvars
!*.tfvars.example
# .terraform.lock.hcl is intentionally committed (provider version pinning)

# Artefatos de execução dos scripts de backfill (one-shot, contêm dados de auditoria)
worker-functions/backfill-worker-names-unresolved.csv
worker-functions/scripts/*.csv
# Diretórios de saída: o padrão *.csv acima só pega a raiz de scripts/, e estes
# snapshots carregam a linha inteira (nome, telefone, observações) — PII sob
# Ley 25.326 a um `git add -A` de distância.
worker-functions/scripts/bulk-archive-output/
worker-functions/scripts/backfill-merged-output/

# Exports do DBA (contêm PII — emails, telefones, nomes de pacientes/prestadores)
data/dba-exports/
.claude/worktrees/
.worktrees/
