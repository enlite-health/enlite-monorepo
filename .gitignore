# Environment
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Build outputs
dist/
build/

# OS
.DS_Store

# Logs
*.log

# Secrets / credentials
.keys/
.mcp.json

# RAG local (vector DB + models cache)
.rag/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfvars
!*.tfvars.example
# .terraform.lock.hcl is intentionally committed (provider version pinning)

# Artefatos de execução dos scripts de backfill (one-shot, contêm dados de auditoria)
worker-functions/backfill-worker-names-unresolved.csv
worker-functions/scripts/*.csv
