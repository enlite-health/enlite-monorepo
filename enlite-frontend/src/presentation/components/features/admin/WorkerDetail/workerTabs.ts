/** As abas da ficha do prestador, na ordem — a mesma lista do `screenRegistry` (`workers.detail`). */
export type WorkerTab = 'encuadres' | 'documents' | 'availability' | 'financial' | 'history';

export const WORKER_TABS: readonly WorkerTab[] = ['encuadres', 'documents', 'availability', 'financial', 'history'];
