/** As abas do detalhe da vaga, na ordem — a mesma lista do `screenRegistry` (`vacancies.detail`). */
export type VacancyTab = 'encuadres' | 'talentum' | 'links' | 'notes';

export const VACANCY_TABS: readonly VacancyTab[] = ['encuadres', 'talentum', 'links', 'notes'];
