export { Button } from './Button';
export type { ButtonProps, ButtonVariant, ButtonSize } from './Button';
// Para o que PARECE botão sem ser `<button>` — os `<Link>` de navegação das
// telas de plantillas usam os MESMOS tokens, em vez de estilo à mão que deriva.
export { buttonClasses } from './buttonClasses';
