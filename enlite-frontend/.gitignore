node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.production.local
.env.development.local
coverage
*.log
.DS_Store
test-results

# Firebase
.firebase/
firebase-debug.log
firebase-debug.*.log

# Playwright
playwright-report/
e2e/.auth/
# 06/09: `e2e/**-snapshots/` SAIU daqui. A regra ignorava as baselines de screenshot, mas 193
# delas já estavam versionadas (entraram antes da linha existir, e .gitignore não afeta arquivo
# já rastreado). O efeito era só sobre spec NOVO: sem baseline no repo, no CI o `toHaveScreenshot`
# falha na 1ª tentativa, ESCREVE o arquivo e passa no retry (`retries: 2`) — a régua vira teatro,
# sempre verde, sem nunca comparar com nada. Baseline de teste visual é código: versiona.
# F5: destino default da evidência da auditoria de UX (o caminho absoluto da máquina de
# UMA pessoa saiu do código). Só nasce quando alguém roda a auditoria à mão.
e2e/__evidence__/
# Visual diff artifacts (regenerated on test failure)
e2e/fixtures/figma/*.actual.png
e2e/fixtures/figma/*.diff.png
e2e/fixtures/figma/*.expected.png

# Provas visuais geradas pelos testes @integration (screenshots/vídeo)
e2e/artifacts

# Saída do comparador desenho x app (e2e/plantillas-vs-desenho): são fotos
# regeneradas a cada execução, como as baselines do Playwright logo acima.
e2e/__comparador__/
